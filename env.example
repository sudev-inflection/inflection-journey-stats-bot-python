# Inflection.io MCP Server Environment Configuration Example
# Copy this file to .env and fill in your actual values

# === Authentication (Required) ===
INFLECTION_EMAIL=your_email@inflection.io
INFLECTION_PASSWORD=your_password

# === API Base URLs (Optional: defaults are recommended) ===
INFLECTION_API_BASE_URL_AUTH=https://auth.inflection.io/api/v1
INFLECTION_API_BASE_URL_CAMPAIGN=https://campaign.inflection.io/api/v2  # Use v2 for reporting endpoints

# === API Endpoints (Optional: do not change unless customizing) ===
INFLECTION_LOGIN_ENDPOINT=/accounts/login
INFLECTION_JOURNEYS_ENDPOINT=/campaigns/campaign.list

# === Server Configuration (Optional) ===
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=8000

# === Logging Configuration (Optional) ===
LOG_LEVEL=INFO

# === HTTP Client Configuration (Optional) ===
API_TIMEOUT=10000  # in milliseconds
MAX_REQUESTS_PER_MINUTE=10

# === Development/Testing (Optional) ===
INFLECTION_TEST_JOURNEY_ID=your_test_journey_id_here